{
  "name": "project-1-book-media-collection-tracker",
  "version": "1.0.0",
  "description": "Full-stack project to track books, movies, and games with FastAPI backend, SQLite database, and React frontend.",
  "scripts": {

    "backend:install": "pip install -r backend/requirements.txt",
    "backend:start": "cd backend && uvicorn main:app --reload --reload-dir .",
    "backend:prestart": "python backend/prestart_backend.py",

    "frontend:start": "cd frontend && npm start",
    "frontend:install": "cd frontend && npm install",

    "start": "concurrently -n BACKEND,FRONTEND -c \"blue,green\" \"npm run backend:start\" \"npm run frontend:start\"",
    "dev": "npm run backend:prestart && npm run start",

    "test": "cd tests && pytest -v"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "devDependencies": {},
  "author": "Rodolfo Baez Jr",
  "license": "ISC"
}
